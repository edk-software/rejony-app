<div class="col-lg-6">
	<div class="box box-primary direct-chat direct-chat-warning">
		<div class="box-header with-border">
			<i class="fa fa-home"></i>
			<h3 class="box-title">{{ title }}</h3>
		</div>
		<!-- /.box-header -->
		<div class="box-body">
			<!-- Conversations are loaded here -->
			<div class="direct-chat-messages" style="height: 373px;">
                {% for comment in comments %}

				<!-- Message. Default to the left -->
				<div class="direct-chat-msg">
					{% if comment.truncatedContent %}
						<div class="direct-chat-info clearfix">
							<a href="{{ spath('area_mgmt_info', {'id': comment.areaId} ) }}">
								<span class="direct-chat-name pull-left">{{ comment.username }} - {{ comment.areaName }}</span>
							</a>
							<span class="direct-chat-timestamp pull-right">{{ ago(comment.createdAt) }}</span>
						</div>
						<!-- /.direct-chat-info -->
						<img class="direct-chat-img" src="{{ asset(avatar(comment, 32)) }}" alt="{{ comment.username }}"><!-- /.direct-chat-img -->
						<div class="direct-chat-text">
							{{ comment.truncatedContent }}
						</div>
					{% else %}
						<div class="direct-chat-info clearfix">
							<a href="{{ spath('area_mgmt_info', {'id': comment.areaId} ) }}">
								<span class="direct-chat-name pull-left">{{ comment.areaName }}</span>
							</a>
						</div>
						<p>{{ 'No comments' | trans }}</p>
					{% endif %}
					<!-- /.direct-chat-text -->
				</div>
				<!-- /.direct-chat-msg -->
                {% endfor %}

			</div>
			<!--/.direct-chat-messages-->

		</div>
		<!-- /.box-body -->
	</div>
</div>
