{% extends 'CantigaCoreBundle:layout:base-layout.html.twig' %}

{% block page_title %}
    {{ 'admin.faq_questions.title' | trans }}
{% endblock %}

{% block page_subtitle %}
    {{ 'admin.faq_questions.subtitle' | trans }}
{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-danger">
                <div class="box-header with-border">
                    <i class="fa fa-lightbulb-o"></i>
                    <h3 class="box-title">{{ 'admin.faq_questions.info' | trans }}</h3>
                </div>
                <div class="box-body no-padding">
                    <table class="table table-hover">
                        <tbody>
                            <tr>
                                <td width="30%">{{ 'admin.faq.category' | trans }}</td>
                                <td>
                                    <a href="{{ path('admin_faq_category_info', { id: item.category.id }) }}">
                                        {{ item.category.name }}
                                    </a>
                                </td>
                            </tr>
                            <tr>
                                <td width="30%">{{ 'admin.faq.topic' | trans }}</td>
                                <td>{{ item.topic }}</td>
                            </tr>
                            <tr>
                                <td width="30%">{{ 'admin.faq.answer' | trans }}</td>
                                <td>{{ item.answer | raw }}</td>
                            </tr>
                            <tr>
                                <td width="30%">{{ 'admin.level' | trans }}</td>
                                <td>{{ levels[item.level] is defined ? levels[item.level] | trans : item.level }}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                <div class="box-footer">
                    <a href="{{ path('admin_faq_question_index', { categoryId: categoryId }) }}"
                        class="btn btn-default btn-sm" role="button">
                        {{ 'admin.back' | trans }}
                    </a>
                    <a href="{{ path('admin_faq_question_edit', { categoryId: categoryId, id: item.id }) }}"
                       class="btn btn-primary btn-sm" role="button">
                        {{ 'admin.edit' | trans }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
